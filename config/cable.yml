# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://:aars@pub-redis-18632.us-east-1-2.1.ec2.garantiadata.com:18632

development:
  adapter: async

test:
  adapter: async

